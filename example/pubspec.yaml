name: example
description: This app demonstrates the capabilities of navigation_history_observer and shows the typical use case for it.

publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  navigation_history_observer: ">=1.0.1"
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
